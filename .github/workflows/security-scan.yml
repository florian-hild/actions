---
name: Security scan
run-name: Security scan

# Trivy over this repository, through the shared workflow.

on:
  pull_request:
    types: [opened, reopened, ready_for_review, synchronize, labeled]
  schedule:
    - cron: "0 6 * * 1" # new findings appear without the code changing
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: security-scan-${{ github.ref }}
  cancel-in-progress: true

jobs:
  trivy:
    name: Trivy
    permissions:
      contents: read
      pull-requests: write
    uses: florian-hild/reusable-workflows/.github/workflows/security-scan.yml@45b7d3e7c50e8e09ce720abb622be2a1d8d1a363 # v1
